{
  "name": "netlify-plugin-netlify-graph",
  "version": "0.0.3",
  "description": "Netlify Graph build plugin",
  "main": "src/index.js",
  "files": [
    "src/**/*.js",
    "src/**/*.mjs",
    "src/**/*.json",
    "src/**/*.sh",
    "src/**/*.html",
    "src/**/*.ejs",
    "manifest.yml"
  ],
  "keywords": [
    "netlify-plugin",
    "netlify"
  ],
  "type": "module",
  "author": "Sean Grove <sean.grove@netlify.com>",
  "license": "MIT",
  "repository": "netlify/netlify-plugin-netlify-graph",
  "directories": {
    "lib": "src",
    "test": "test"
  },
  "engines": {
    "node": ">=14.18.3"
  },
  "scripts": {
    "build": "netlify-build",
    "prettier": "prettier --ignore-path .gitignore --write --loglevel warn \"{.github,src,init}/**/*.{js,md,yml,json}\" \"*.{js,md,yml,json}\"",
    "release": "release-it"
  },
  "devDependencies": {
    "execa": "^5.0.0",
    "prettier": "^2.5.1",
    "release-it": "^14.0.3",
    "typescript": "^4.5.2"
  },
  "dependencies": {
    "@netlify/build": "^26.3.14",
    "@netlify/config": "^17.0.18",
    "@netlify/framework-info": "^9.0.2",
    "netlify-onegraph-internal": "0.2.0"
  }
}
